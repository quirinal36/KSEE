<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
  정적 페이지 콘텐츠 매퍼. 모든 입력을 #{} 로 바인딩하여 안전하게 처리한다.
-->
<mapper namespace="page_content_sql">

	<select id="selectByKey" parameterType="page_content" resultType="page_content">
		SELECT id, content_key, title, content, content_en, udate
		FROM page_content
		<where>
			content_key = #{content_key}
		</where>
	</select>

	<insert id="insert" parameterType="page_content" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO page_content (content_key, title, content, content_en)
		VALUES (#{content_key}, #{title}, #{content}, #{content_en})
	</insert>

	<update id="update" parameterType="page_content">
		UPDATE page_content
		<set>
			<if test="title != null">title = #{title},</if>
			<if test="content != null">content = #{content},</if>
			<if test="content_en != null">content_en = #{content_en},</if>
		</set>
		<where>
			content_key = #{content_key}
		</where>
	</update>

</mapper>
